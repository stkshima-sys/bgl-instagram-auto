# Baseball Girls LIFE Instagram自動運用システム セットアップ・運用手順

Draft公式の「Instagram自動投稿」システムと同じ仕組みの、Baseball Girls LIFE（@baseball_girls_life）専用版。
**アカウント・リポジトリ・スプレッドシート・トークンはすべてDraft公式と別に用意する。**

## 1. システム全体アーキテクチャ

```
┌──────────────── GitHub Actions（すべて自動実行・PCオフでも動く） ────────────────┐
│                                                                                  │
│ 【フィード①】毎日 11:30 JST 開始 → 12:00頃 投稿（noon・実用リスト系）              │
│ 【フィード②】毎日 18:30 JST 開始 → 19:00頃 投稿（evening・共感エモ系）             │
│   投稿テーマ設定.md＋改善方針.md を読む                                            │
│   → カテゴリ選択（グルメ30/コーデ20/一人観戦15/初心者15/球場紹介10/推し活10）       │
│   → GPT-4.1で企画・キャプション・タグ生成                                          │
│   → gpt-image-1で表紙背景を生成 → sharpでパステル×明朝体の雑誌風スライドに合成      │
│   → GitHubへcommit（公開rawURL化） → Instagram Graph APIでカルーセル投稿           │
│   → Google Sheets「Instagram_Posts」に記録                                        │
│   ※noon実行時はストーリーズ台本（6件）も自動生成 → queue/stories/（投稿は手動）    │
│                                                                                  │
│ 【リール】毎日 20:20 JST 開始 → 21:00頃 投稿                                       │
│   カテゴリ選択 → 企画＋キャプション＋Kling/Veo/Higgsfield用英語プロンプト生成        │
│   → (A) Replicate設定済み: Kling APIで動画自動生成 → commit → リール投稿           │
│   → (B) 未設定: queue/reels/のプロンプトで手動生成 → videos/に置くと翌21:00に投稿   │
│                                                                                  │
│ 【計測】毎日 23:30 JST                                                            │
│   直近14日の投稿の Reach/Views/Likes/Comments/Shares/Saves/Profile Visits/        │
│   Follows を取得 → Sheets更新。フォロワー数は「Account_Daily」に日次記録            │
│                                                                                  │
│ 【分析・改善】毎週月曜 9:00 JST                                                    │
│   Sheets全データを集計 → GPT-4.1が分析レポート＋改善案10個＋ジャンル比率提案         │
│   → reports/ に保存 → 改善方針.md を自動上書き → 翌日以降の投稿に自動反映           │
└──────────────────────────────────────────────────────────────────────────────────┘
```

## 2. Draft公式版との違い

| 項目 | Draft公式版 | BGL版（本システム） |
|---|---|---|
| アカウント | Draft公式 | @baseball_girls_life（新規） |
| フィード投稿 | 1日1回 12:00 | **1日2回 12:00 / 19:00** |
| ブランド | Draft宣伝込み | ライフスタイルメディア。Draft誘導はCTA1行のみ |
| デザイン | ネイビー×ピンク | 白・ベージュ・パステルピンク・水色×明朝体 |
| ストーリーズ | なし | 台本を毎朝自動生成（queue/stories/、投稿は手動） |
| カテゴリ比率 | あるある20/グルメ20… | グルメ30/コーデ20/一人観戦15/初心者15/球場紹介10/推し活10 |

## 3. 初期セットアップ

### 3-0. Instagramアカウント作成（最初にやる）
1. Instagramで新規アカウント作成（例: @baseball_girls_life）
2. プロフィール設定:
   - 名前: Baseball Girls LIFE｜野球女子のライフスタイルマガジン
   - 自己紹介: 球場グルメ・観戦コーデ・一人観戦のコツを毎日お届け🌸 など
   - リンク: `https://draft-match.com/?utm_source=instagram&utm_medium=bgl`（UTM付きで流入計測）
3. プロアカウントに切り替え、**Draft公式とは別の**Facebookページを作成して連携

### 3-1. GitHubリポジトリ作成
1. GitHubで**公開（public）リポジトリ**を新規作成（例: `bgl-instagram-auto`）
   - ⚠️ Instagram APIは公開URLからしか画像/動画を取得できないため、**publicが必須**
2. このフォルダの中身をすべてpush:
   ```
   git init
   git add .
   git commit -m "initial"
   git branch -M main
   git remote add origin https://github.com/<ユーザー名>/bgl-instagram-auto.git
   git push -u origin main
   ```

### 3-2. Instagram Graph API（Meta）
1. https://developers.facebook.com → 既存のMetaアプリを使うか新規作成（タイプ「ビジネス」）
2. グラフAPIエクスプローラで以下の権限を付けてユーザートークンを発行:
   `instagram_basic, instagram_content_publish, instagram_manage_insights, pages_read_engagement, business_management`
3. 長期トークンに交換（60日有効）:
   ```
   https://graph.facebook.com/v23.0/oauth/access_token?grant_type=fb_exchange_token&client_id=<アプリID>&client_secret=<アプリシークレット>&fb_exchange_token=<短期トークン>
   ```
4. **BGL用の**ビジネスアカウントIDを取得:
   ```
   https://graph.facebook.com/v23.0/me/accounts?access_token=<トークン>   ← BGL用ページID取得
   https://graph.facebook.com/v23.0/<ページID>?fields=instagram_business_account&access_token=<トークン>
   ```

### 3-3. Google Sheets
1. Google Cloudの既存プロジェクト（Draft公式版で作成済みならそれでOK）でサービスアカウントを流用可
2. **BGL専用の新規スプレッドシート**を作成し、サービスアカウントのメールアドレスに編集者で共有
   （シート「Instagram_Posts」「Account_Daily」は初回実行時に自動作成される）

### 3-4. GitHub Secrets / Variables設定

リポジトリ → Settings → Secrets and variables → Actions

**Secrets（機密）:**

| 名前 | 値 |
|---|---|
| `OPENAI_API_KEY` | OpenAIのAPIキー（Draft公式版と共通でOK） |
| `INSTAGRAM_ACCESS_TOKEN` | **BGL用**長期アクセストークン |
| `INSTAGRAM_BUSINESS_ACCOUNT_ID` | **BGL用**IGビジネスアカウントID |
| `GOOGLE_SHEETS_ID` | **BGL専用**スプレッドシートID |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | サービスアカウントJSONの中身をそのまま貼る |
| `REPLICATE_API_TOKEN` | （任意）動画完全自動化する場合のみ |

**Variables（非機密）:**

| 名前 | 値 |
|---|---|
| `MEDIA_BASE_URL` | `https://raw.githubusercontent.com/<ユーザー名>/bgl-instagram-auto/main` |
| `REPLICATE_VIDEO_MODEL` | （任意）既定 `kwaivgi/kling-v2.1` |

### 3-5. 動作テスト
Actionsタブ → 各ワークフロー → 「Run workflow」で手動実行して確認。
フィードはslot（noon/evening）を選んで実行できる。
順番: ①フィード（noon） → ②インサイト取得 → ③リール

ローカルでテストする場合（要 Node.js 20以上）:
```
npm install
copy .env.example .env   ← 値を記入
npm run carousel:generate -- noon
npm run carousel:post -- noon
```

## 4. cronスケジュール（GitHub Actions・UTC指定）

| ワークフロー | cron (UTC) | JST | 内容 |
|---|---|---|---|
| carousel.yml | `30 2 * * *` | 毎日11:30〜 | フィード①生成→12:00頃投稿＋ストーリーズ台本 |
| carousel.yml | `30 9 * * *` | 毎日18:30〜 | フィード②生成→19:00頃投稿 |
| reel.yml | `20 11 * * *` | 毎日20:20〜 | 企画→動画→21:00頃投稿 |
| insights.yml | `30 14 * * *` | 毎日23:30 | 指標取得→Sheets |
| weekly.yml | `0 0 * * 1` | 月曜9:00 | 分析→改善方針更新 |

※GitHub Actionsのcronは数分〜遅延することがある（無料枠の仕様）。

## 5. ストーリーズの運用（手動）
Instagram APIはアンケート・質問・クイズなどのスタンプ付きストーリーズを投稿できない。
そのため毎朝のnoon実行時に `queue/stories/日付.md` へ6件分の台本
（時刻・種類・文言・選択肢・メモ付き）が自動生成されるので、
**スマホからコピペで投稿する**（1日3〜5分の作業）。

## 6. リール動画の運用（2モード）

**A. 完全自動（推奨）**: `REPLICATE_API_TOKEN` を設定。
毎日20:20にKling（Replicate経由）で動画生成→そのまま投稿。
費用目安: Kling v2.1で1本あたり$0.25〜1程度 × 月30本。

**B. 半自動（品質重視）**: トークン未設定。
`queue/reels/日付.json` に3種の英語プロンプト（Kling/Veo/Higgsfield用）が毎日生成されるので、
好きなツールで動画を作り `videos/日付.mp4` としてpushしておくと、次の21:00に自動投稿される。
動画が無い日は投稿をスキップする（エラーにはならない）。

## 7. 日常運用
- 基本は放置でOK（ストーリーズの手動投稿のみ）。**PCがオフでも動く**
- 戦略を変えたい: `prompts/投稿テーマ設定.md` を編集してpush
- 今週だけ手動指示したい: `prompts/改善方針.md` に直接書く（月曜に自動上書きされる点に注意）
- 投稿を止めたい: GitHub → Actions → 対象ワークフロー → 「…」→ Disable workflow
- 実績確認: Googleスプレッドシート「Instagram_Posts」「Account_Daily」
- 週次レポート: `reports/週次分析_日付.md`
- Draft流入確認: Draft側のアクセス解析で `utm_medium=bgl` の流入を見る

### 費用目安（月）
- OpenAI: テキスト生成＋画像2枚/日 ≒ $10〜20
- Replicate（動画自動生成する場合）: $10〜30
- GitHub Actions: publicリポジトリは無料

## 8. エラー時の復旧手順

### 投稿が失敗した（Actionsが赤い）
1. Actionsタブ → 失敗したrun → ログのエラーメッセージを確認
2. そのまま「Re-run failed jobs」で再実行（キューが残っているので再投稿できる）
3. フィードのキューは `queue/carousel/日付_slot.json` に残る。当日中なら再実行でOK

### よくあるエラーと対処

| エラー | 原因 | 対処 |
|---|---|---|
| `Error validating access token` / code=190 | トークン期限切れ（60日） | 手順3-2で再発行してSecretsを更新 |
| `Media upload failed` / メディア処理ERROR | rawURLがまだ反映されていない | Re-run（sleepを120秒に増やすのも有効） |
| `(#10) Application does not have permission` | 権限不足 | トークンに `instagram_content_publish` があるか確認 |
| `The user has exceeded the number of posts` | 24時間の投稿上限（API上限50件/日） | 通常発生しない。翌日回復 |
| `Graph API error ... code=4` | レート制限 | 1時間待って再実行 |
| Sheets `PERMISSION_DENIED` | サービスアカウント未共有 | シートを編集者権限で共有し直す |
| 画像の日本語が豆腐（□）になる | フォント未インストール | carousel.ymlのfonts-noto-cjkステップが動いているか確認 |
| 動画生成タイムアウト | Replicate混雑 | Re-run。続く場合は `REPLICATE_VIDEO_MODEL` を変更 |

### アクセストークンの定期更新（重要・60日ごと）
長期トークンは約60日で失効する。**毎月1回、月初に再発行→Secrets更新**を推奨。
Draft公式版のトークン更新と同じタイミングでまとめてやると忘れない。

### 完全に作り直したい場合
`queue/` `posted/` `media/` `videos/` を削除してpushすれば、翌日からクリーンに再開する。
Sheetsのデータはそのまま残す（分析の継続性のため消さないこと）。
